import QtQuick 2.14

import Theme 1.0

//! A horizontal line which shell be used as devider if one is needed.
Rectangle {
    id: root

    implicitHeight: 1
    implicitWidth: 320

    color: Theme.foreground
    opacity: 0.2
}
